Physical design (OpenROAD/OpenLane, GDSII) is a **Stage 2** deliverable
per the challenge document, not Stage 1. This directory is intentionally
empty in this submission.

Stage 1 focuses on RTL design and functional verification (see
`../reports/design_document.md`). A synthesis/lint sanity check (Yosys)
was run as an extra, not a substitute for the Stage 2 physical-design
flow — see `../synthesis/yosys_lint_summary.md`.
